--Index 사용하기
SELECT INDEX_NAME, TABLE_NAME, COLUMN_NAME
FROM USER_IND_COLUMNS
WHERE TABLE_NAME IN ('EMP', 'DEPT');

--인덱스 설정 전과 설정 후 조회 속도 비교
DROP TABLE EMP01;

CREATE TABLE EMP01
AS
SELECT * FROM EMP;

INSERT INTO EMP01
SELECT * FROM EMP01;

SELECT * FROM EMP01;

INSERT INTO EMP01 (EMPNO, ENAME)
VALUES (1111, 'SYJ');

--시간체크하는 명령어
SET TIMING ON;
SELECT DISTINCT EMPNO, ENAME
FROM EMP01
WHERE ENAME = 'SYJ';

--INDEX 생성
CREATE INDEX IDX_EMP01_ENAME
ON EMP01(ENAME);

SELECT INDEX_NAME, TABLE_NAME, COLUMN_NAME
FROM USER_IND_COLUMNS
WHERE TABLE_NAME IN('EMP01');

--생성한 INDEX를 사용하는 곳은 SELECT 명령어의 WHERE 조건절 내부에서 주로 사용
SET TIMING ON;
SELECT DISTINCT EMPNO, ENAME
FROM EMP01
WHERE ENAME='SYJ';


--INDEX 삭제하기
DROP INDEX IDX_EMP01_ENAME;











